{
  "name": "vivo-factory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build:css": "postcss src/input.css -o public/styles.css --minify",
    "dev": "npm run build:css && node src/server.js",
    "start": "node src/server.js",
    "bootstrap": "node src/bootstrap.js",
    "generate:stacks": "node src/generate-stacks.js",
    "deploy:stacks": "node src/deploy-stacks.js"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.10",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^3.4.19"
  }
}
